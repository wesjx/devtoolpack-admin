// ficheiro: next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // As suas configurações do Next.js vão aqui.
  // Se não tiver nenhuma configuração customizada, pode deixar este objeto vazio.
  // Exemplo:
  // images: {
  //   domains: ['example.com'],
  // },
};

export default nextConfig;